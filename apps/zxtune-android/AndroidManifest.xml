<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="app.zxtune"
      android:versionCode="@integer/versionCode"
      android:versionName="@string/versionName">
    <uses-sdk android:minSdkVersion="8" />
    <application
          android:label="@string/app_name"
          android:icon="@drawable/ic_launcher"
          android:allowBackup="true">
        <activity
              android:name=".CurrentlyPlayingActivity"
              android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
              android:name=".PlayFileActivity"
              android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"></action>
                <category android:name="android.intent.category.DEFAULT"></category>
                <category android:name="android.intent.category.BROWSABLE"></category>
                <data android:host="*" android:scheme="file" android:mimeType="*/*" android:pathPattern=".*" />
            </intent-filter>
        </activity>
        <service 
              android:name=".PlaybackService"
              android:process=":service">
        </service>
        <provider
              android:name="app.zxtune.playlist.Provider"
              android:authorities="app.zxtune.playlist"
              android:exported="false"
              android:multiprocess="true"
              android:process=":service">
        </provider>
    </application>
</manifest>
