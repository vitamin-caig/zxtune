binary_name := benchmark
dirs.root := ../..
source_dirs := .

libraries.common = devices_aym devices_z80 l10n_stub sound strings tools
libraries.3rdparty = z80ex

libraries := benchmark
depends := apps/benchmark/core

include $(dirs.root)/makefile.mak
