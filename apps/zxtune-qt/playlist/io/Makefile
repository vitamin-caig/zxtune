# XSPF versions:
# 0 - initial, no ATTRIBUTE_VERSION tag in playlist properties
# 1 - 'percenting' all the text fields

library_name := playlist_io
path_step := ../../../..

use_qt := 1

source_dirs := .

text_files := tags/xspf tags/ayl

includes.dirs += ../..

include $(path_step)/makefile.mak
