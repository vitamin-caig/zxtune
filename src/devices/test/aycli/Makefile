binary_name := aycli
dirs.root := ../../../..
source_dirs := .

libraries.common = async core debug devices_aym l10n_stub parameters platform sound sound_backends strings tools

libraries.windows = ole32 user32
libraries.mingw = ole32

include $(dirs.root)/makefile.mak
