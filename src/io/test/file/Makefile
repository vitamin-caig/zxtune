binary_name := io_test_file
dirs.root := ../../../..
source_dirs := .

libraries.common = binary debug io l10n_stub parameters platform strings tools

libraries.windows := advapi32

libraries.boost := filesystem system

include $(dirs.root)/makefile.mak
