binary_name := module_info
path_step := ../../../..
source_files := main

libraries := analysis binary core devices formats io sound tools z80ex
depends := src/analysis src/binary src/core src/devices src/formats src/io src/sound src/tools 3rdparty/z80ex

windows_libraries := advapi32

include $(path_step)/makefile.mak
