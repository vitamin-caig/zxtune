binary_name := formats_test_zip
path_step := ../../../..
source_files := test

libraries := io binary formats tools
depends := src/formats src/binary src/io src/tools

windows_libraries := advapi32

include $(path_step)/makefile.mak
