binary_name := formats_test_lha
dirs.root := ../../../..
source_dirs := .

libraries.common = binary binary_format debug formats_archived formats_packed strings tools
libraries.3rdparty = lhasa

libraries.windows = oldnames

include $(dirs.root)/makefile.mak
