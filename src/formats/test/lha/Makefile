binary_name := formats_test_lha
path_step := ../../../..
source_files := test

libraries := io binary formats tools lhasa
depends := src/formats src/binary src/io src/tools 3rdparty/lhasa/lib

windows_libraries := advapi32 oldnames

include $(path_step)/makefile.mak
