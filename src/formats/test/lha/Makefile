binary_name := formats_test_lha
path_step := ../../../..
source_files := test

libraries := binary formats tools lhasa
depends := src/formats src/binary src/tools 3rdparty/lhasa/lib

windows_libraries = oldnames

include $(path_step)/makefile.mak
